# HomeWorkGo

## Description:
```Text
1) створити акаунт на github
2) стоврити новий репозиторій на github
3) сворити 5 гілок (таким чином щоб кожна гілка мала свої унікальні коміти)
4) хочаб 2 гілки мержити між собою
5) злити усі гілки у main гілку через створення пул реквестів
```

## Git commands:
```bash
git clone git@github.com:YuriiOsipchuk/HomeWorkGo.git
git log --oneline --decorate --graph --all
git branch -a
git branch tester
git checkout tester
git checkout -b manager
git checkout -b developer01
git checkout -b developer02
git checkout -b developer03
git status
git add README.md
git add .
git commit -m "Start project"
```
